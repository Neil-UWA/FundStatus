var chai    = require('chai'),
    expect  = chai.expect,
    util    = require('util'),
    sinon   = require('sinon'),
    FundRunner = require('../lib/fund_runner');

describe('FundRunner', function(){
  describe('constructor', function(){
    context('when no fund code is passed', function(){
      it('returns default fund code');
    });

    context('when fund codes are passed', function(){
      it('returns default fund code');
    });
  });
});
